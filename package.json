{
  "name": "demos-red-mortgage",
  "version": "2.0.0",
  "private": true,
  "description": "Full-stack mortgage origination platform — Java/Spring Boot backend + Next.js frontend",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "build:frontend": "cd frontend && npm run build",
    "lint:frontend": "cd frontend && npm run lint",
    "typecheck:frontend": "cd frontend && npm run typecheck",
    "build:backend": "cd backend && mvn -B package -DskipTests",
    "test:backend": "cd backend && mvn -B test",
    "validate": "npm run lint:frontend && npm run typecheck:frontend && npm run test:backend"
  }
}
